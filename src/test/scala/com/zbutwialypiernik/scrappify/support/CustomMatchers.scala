package com.zbutwialypiernik.scrappify.support

import org.scalatest.matchers.should.Matchers

trait CustomMatchers {
  this: Matchers =>

  //def matchErrorResponse(statusCode: StatusCode, message: String): Matcher[ErrorResponse] = {
 //   in should have(Symbol("statusCode")(statusCode), Symbol("message")(message)
  //)


}
